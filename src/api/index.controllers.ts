import { healthCheck } from './components/health-check/health-check.controller';
import { postMessage } from './components/message/message.controller';

export default {
  healthCheck,
  postMessage
};
